/**
 * 1. first firebase npm install firebase
 * 2. console.firebase.google.com create a project
 * 3. Register project
 * 4. get firebase config and do not save config on the clint side
 * 5. export auth from the init file
 * 6. create auth where it will be added
 * 7. in the console authentication > started > enable google provider
 */